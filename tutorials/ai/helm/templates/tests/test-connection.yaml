apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ai.fullname" . }}-test-connection"
  labels:
    {{- include "ai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ai.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
