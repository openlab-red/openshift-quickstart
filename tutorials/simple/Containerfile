# Use Red Hat Universal Base Image 9 Minimal as the base image
FROM registry.redhat.io/ubi9-minimal

ENV MY_VAR="Hello, Podman!"

# Set the default command to print "Hello World" when container starts
ENTRYPOINT [ "echo", "Hello World!" ] 